# API token (never commit)
make-ai-model-manager.secret.json

# Local config state (org IDs, team IDs — personal to each user)
# Uncomment if you want users to start with a fresh config:
# make-ai-model-manager.json

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
